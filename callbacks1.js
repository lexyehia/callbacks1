// The second argument/parameter is expected to be a function

["Alice", "Bob", "Waldo", "Winston"].forEach((element, index) => {
    if (element === "Waldo") { console.log(`Found Waldo at index ${index}.`); }
});


